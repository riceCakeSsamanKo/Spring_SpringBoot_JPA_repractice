- Repository 패키지 내부에 별도로 query 패키지를 만든 이유

이 패키지에서는 특정 화면과 관련된 별도의 Repository를 정의함으로써, 화면과 관련된 것들은 따로
여기에 보관하고 중요 비즈니스 로직은 그냥 repository 패키지에서 저장하게된다.
Api의 라이프 사이클과 핵심 비즈니스 로직의 라이프 사이클이 다른 경우가 많고,
그 때문에 이들을 별도로 관리하는 것이 좋다.
